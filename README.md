
**Русский** | [English](/README.en_US.md)

---

# MineTgBridge

**MineTgBridge** — это плагин для Minecraft (Paper / Spigot), который **синхронизирует чат сервера Minecraft с группой Telegram**.

Игроки в Minecraft видят сообщения из Telegram, а сообщения из игрового чата автоматически отправляются в Telegram-группу.

---

## ✨ Возможности

- 🔁 Двусторонняя синхронизация чата:
  - Minecraft → Telegram
  - Telegram → Minecraft
- 👥 Отображение никнеймов пользователей Telegram
- 🛑 Защита от зацикливания сообщений (бот не пересылает свои же сообщения)
- 🖼️ Поддержка нетекстовых сообщений (фото, стикеры, голосовые и др.)
- ⚙️ Гибкая настройка форматирования сообщений
- ⏱ Настраиваемый интервал проверки новых сообщений

---

## ⚙️ Конфигурация (`config.yml`)

```yaml
# v1.4 - версия конфига, обновите если используете старую

telegram:
  token: "BOT_TOKEN"        # Токен Telegram-бота (BotFather)
  chat-id: "@group_link"    # @group_link или числовой ID
  group-link: "group_link"  # Название группы БЕЗ @
  send-all-than-text: true  # Отправлять ли нетекстовые сообщения
  time-check: 5             # Интервал проверки сообщений (в секундах)

message:
  telegram: "<{user}> "     # Формат сообщений из Telegram
  tg-check: "[TG"           # Префикс для защиты от зацикливания
  server: "<{player}> {message}" # Формат сообщений из Minecraft
````

---

## 🔧 Параметры конфигурации

### `telegram.token`

Токен Telegram-бота.
Получается через **@BotFather**.

---

### `telegram.chat-id`

ID чата, куда бот будет отправлять сообщения.

Варианты:

* Группа: `@group_link`
* Личные сообщения: `123456789`

---

### `telegram.group-link`

Юзернейм Telegram-группы **без символа `@`**.
Используется для фильтрации сообщений (чтобы бот слушал только нужную группу).

---

### `telegram.send-all-than-text`

Определяет, как обрабатывать **нетекстовые сообщения**:

* `true` — отправлять описание (`Изображение`, `Стикер`, `Голосовое сообщение`)
* `false` — игнорировать такие сообщения

---

### `telegram.time-check`

Интервал (в секундах) между запросами к Telegram API.

Рекомендуется: `3–10` секунд.

---

### `message.telegram`

Формат сообщений, **приходящих из Telegram в Minecraft**.

Плейсхолдеры:

* `{user}` — имя пользователя Telegram

Пример:

```
<{user}> 
```

---

### `message.server`

Формат сообщений, **отправляемых из Minecraft в Telegram**.

Плейсхолдеры:

* `{player}` — ник игрока
* `{message}` — текст сообщения

---

### `message.tg-check`

Строка-маркер для защиты от зацикливания сообщений
(чтобы бот не пересылал сообщения, отправленные самим собой).

---

## 📦 Установка

1. Скачайте `MineTgBridge.jar`
2. Поместите файл в папку `plugins`
3. Запустите сервер (создастся `config.yml`)
4. Настройте конфиг
5. Перезапустите сервер

---

## 🧩 Требования

* Minecraft **Paper / Spigot**
* Java **17+**
* Открытый доступ к `api.telegram.org`

---

## 📄 Лицензия

Проект распространяется под лицензией **MIT**
